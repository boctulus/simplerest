# Core

Core guides are being reconstructed from the implementations in `src/framework/` and their application configuration. See the [architecture overview](../architecture.md) for the currently verified entry-point and bootstrap relationships.

Routing, request/response behavior, middleware, and dependency wiring will be published here after their source and tests are traced.
