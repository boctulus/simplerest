# Response documentation moved

The former [Response guide](../audit/pending/framework/Response.md) is retained as unverified audit input. The lifecycle page documents only `Response::flush()` in dispatch; the `Response` helper and formatting APIs have not completed claim-level audit.
