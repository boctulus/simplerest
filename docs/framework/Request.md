# Request documentation moved

The former [Request guide](../audit/pending/framework/Request.md) is retained as unverified audit input. The lifecycle page documents only `RequestHandler::parse()`; the `Request` helper API has not completed claim-level audit.
