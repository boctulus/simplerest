# HTTP and REST API

## Verified documentation

No HTTP or REST usage guide has completed audit yet.

## Pending audit

Endpoint resolution, request parameters, serialization, authentication, authorization, validation, and automatic CRUD coverage are being traced through implementation and configuration. The broad claims in the old automatic-endpoint guide remain unverified until an end-to-end example is run. See the [pending audit area](../audit/pending/README.md).
