# HTTP and REST API

REST endpoint resolution, request parameters, serialization, authentication, authorization, and validation are separate contracts. These guides will be published after each contract is traced through its implementation and configuration.

The broad claims in existing automatic-endpoint guides remain unverified until an end-to-end example is run. See the [audit register](../audit/README.md).
