# Command line

The repository exposes a `com` CLI entry point. A complete command reference requires discovery from the current command registry and verification of each documented invocation; older command counts and lists are not authoritative.
